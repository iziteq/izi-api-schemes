{
	"description": "Audio objects",
    "type": "array",
    "minItems": 1,
	  // Latest CMS versions allow to have only one audio, however, there is content with several audio 
    // "maxItems": 1,
    "items": {
  		"type": "object",
  		"additionalProperties": false,
  		"required": ["uuid", "type", "order", "duration", "hash", "size"],
  		"properties": {
  			"uuid"     : { "type": "string"  },
  			"type"     : { "type": "string"  },
  			"hash"     : { "type": "string"  },
  			"size"     : { "type": "number"  },
  			"order"    : { "type": "integer" },
  			"duration" : { "type": "integer" },
  			"url"      : { "type": "string"  }
  		}
    }
}